import { TalosClient, ApiError } from '../src';test('pagination', async () => { let f=jest.fn(); global.fetch=f as any; let c=new TalosClient({baseUrl:'x'}); f.mockResolvedOnce(new Response(JSON.stringify({items:[1],nextCursor:'n'}),{status:200})); await expect(c.getPaginated('/x')).resolves.toEqual({items:[1],nextCursor:'n'}); f.mockResolvedOnce(new Response(JSON.stringify({items:[],nextCursor:null}),(status:200})); await expect(c.getPaginated('/x')).resolves.toEqual({items:[],nextCursor:null}); f.mockResolvedOnce(new Response('bad',{status:200})); await expect(c.getPaginated('/x')).rejects.toBeInstanceOf(ApiError); f.mockRejectedOnce(new TypeError('fail')); await expect(c.getPaginated('/x')).rejects.toBeInstanceOf(ApiError); });